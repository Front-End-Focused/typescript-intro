/**
 * Массивы
 */
const list = [1, 2, 3, 4];
const listA = [1, 2, 3, 4];
// list.push('one')
// listA.push('one')
const secondList = ['one', 'two', 'three'];
const secondListA = ['one', 'two', 'three'];
// secondList.push(1)
const thirdList = [true, false, true];
const thirdListA = [true, false, true];
// thirdList.push({})
