/**
 * Массивы
 */
const list: number[] = [1,2,3,4]
const listA: Array<number> = [1,2,3,4]

// list.push('one')
// listA.push('one')

const secondList: string[] = ['one', 'two', 'three']
const secondListA: Array<string> = ['one', 'two', 'three']

// secondList.push(1)

const thirdList: boolean[] = [true, false, true]
const thirdListA: Array<boolean> = [true, false, true]

// thirdList.push({})